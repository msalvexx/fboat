version: "3"

services:
  # frontend:
  #   build: .
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - .env
  db:
    image: mysql:8.0
    restart: always
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: test
      MYSQL_ROOT_PASSWORD: ''
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
